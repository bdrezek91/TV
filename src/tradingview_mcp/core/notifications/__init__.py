"""Telegram alerting (Block 3). `TELEGRAM_ENABLED` stays `false` by default
(see `.env.example`) — this package only wires up the mechanism; no bot
token is ever committed and no real HTTP call is made unless an operator
explicitly enables it with their own token."""
