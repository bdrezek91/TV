"""Replay Engine (Block 3): deterministic, no-look-ahead backtesting over
already-persisted market data, reusing the exact same Strategy Engine and
Paper Trading Engine code paths as live paper trading — not a separate
reimplementation that could silently drift from production behaviour."""
